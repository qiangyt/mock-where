{
    "version": "0.2.0",
    "configurations": [

        {
            "type": "node",
            "request": "launch",
            "name": "launch the application",
            "program": "${workspaceRoot}/src/app.js"
        }
    ]
}